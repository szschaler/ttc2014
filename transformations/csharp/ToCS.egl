[%
  for (cl in Model!Class.all()) {
    var t := TemplateFactory.load('CSOneClass.egl');
    t.populate ('currentClass', cl);
    t.generate ('../../src/' + cl.name + '.cs'); // TODO: For testing purposes, use a unique name here
  }
%]