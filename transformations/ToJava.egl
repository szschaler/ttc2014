[%
  for (cl in Model!Class.all()) {
    var t := TemplateFactory.load('JavaOneClass.egl');
    t.populate ('currentClass', cl);
    t.generate ('../src/' + cl.name + '.java'); // TODO: For testing purposes, use a unique name here
  }
%]