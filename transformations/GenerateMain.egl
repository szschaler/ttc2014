[% // Generate main method
  for (mdl in Model!Model) {
    var t := TemplateFactory.load('JavaMainMethod.egl');
    t.populate ('currentClass', mdl.topClass);
    t.generate ('../src/' + mdl.topClass.name + '.java');
  }
%]